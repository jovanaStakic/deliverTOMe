import { BorderDirectiveDirective } from './border-directive.directive';

describe('BorderDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new BorderDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
